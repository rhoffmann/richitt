mutation RegisterUser($options: UserRegisterInput!) {
  register(options: $options) {
    errors {
      field
      message
    }
    user {
      id
      createdAt
      username
      email
    }
  }
}
